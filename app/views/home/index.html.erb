<h3>
<% if current_user.teams.present? %>
	Fala <%= current_user.name %>, torcedor(a) do <%= current_user.teams.first.name %>, você está no bolao du bah!!!<br/></span>
<% else %>
 	Fala <%= current_user.name %>, você está no bolao du bah!!!<br/></span>
<% end %>
</h3>

<p>O jiow se você já sabe as regras do bolão pode clicar em apostar logo e não perder tempo ledo regras hehehe !!!</p>

<p>Agora se tu é fraco e não conhece ainda clica no botão AZUL que esta escrito REGRAS lá para vê como eh essa bagaça!!</p>

<p>
	<%= link_to 'Apostar', apostar_path, :class=> 'btn btn-success btn-large' %>
	<%= link_to 'Regras', regras_path, :class=> 'btn btn-info btn-large' %>
</p>


